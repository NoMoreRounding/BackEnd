server:
  port: 1998  # Spring boot Server port

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://146.56.158.121:3306/nomorerounding?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: nmrdb
    password: rmfThrhd!@

  jpa:
    hibernate:
#      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

  jwt:
    secret: shahdjfkdnseldtjqjwkrdjqwnd
    subject: nomoreroundingJwt
    claims:
      user-id: uid
      authority: ath
      email: eml
      platform-id: pid

  mail:
    host: smtp.gmail.com
    port: 587
    username: nomorerounding@gmail.com
    password: vpvnjmtzixvvspam
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

loggin.level:
  org.hibernate.type: trace

